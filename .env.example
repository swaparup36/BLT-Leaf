## .env.example - environment variables for BLT-Leaf
# Copy this file to .env (the setup script will do this and overwrite .env)
# Fill values or leave blank for defaults. Do NOT commit your real secrets.

# D1 database id (will be populated by setup-local.sh)
D1_DATABASE_ID=

# GitHub personal access token (recommended, increases rate limits)
# Create a token with `repo` and `read:org` scopes as needed.
GITHUB_TOKEN=

# Cloudflare account ID (optional, used by some workflows)
CLOUDFLARE_ACCOUNT_ID=

# Optional: API token for Wrangler if you prefer non-interactive login
WRANGLER_API_TOKEN=
